"""Le vocabulaire du socle : contrats, AST, erreurs à cause fermée, faits.

Rien n'est réexporté ici : chaque module importe le sous-module qu'il lit — `kernel.contracts`,
`kernel.codeview`, `kernel.errors`, `kernel.facts`, `kernel.protocol`. Ce fichier existe pour que
`kernel` soit un paquet régulier comme les dix autres modules, condition d'une collecte de tests
sans collision de noms.
"""
